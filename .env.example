# visual-verify 视觉检验脚本配置（复制为 skills/visual-verify/scripts/.env 后填写）
# 环境变量优先于本文件。切勿把填好的 .env 提交到 git。

# 必填：你的视觉模型 API key
VISION_API_KEY=

*** 可选：任何兼容 /chat/completions 的网关（默认 OpenAI）
VISION_BASE_URL=https://api.openai.com/v1

# 可选：需支持图像输入的模型名（默认 gpt-4o）
VISION_MODEL=gpt-4o
